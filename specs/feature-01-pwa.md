configure pwa in this project nextjs 16 static export using folder docs as root

use public/icon-192x192.png and public/icon-512x512.png as icons

use @serwist/next @serwist/precaching @serwist/sw existing in package.json

config next.config.ts

create Service Worker

create Web App Manifest

register service worker in app/layout.tsx

add meta tags in app/layout.tsx

show instructions to test